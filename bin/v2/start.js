/**
 * Placeholder start function for version 3.
 */
export default function start({ jsFilePath, inCheckLines, showLog }) {
    if (showLog) {
        console.log(`v3 start called on ${jsFilePath}`);
    }
}
